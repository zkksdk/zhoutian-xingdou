/* ============================================================
   周天星斗大阵 · 3. 大阵根节点
   源文件分节 #3 —— 仅加入了模块 import / export，逻辑未改动
   ============================================================ */
import * as THREE from 'three';
import { scene } from './01-core.js';

/* ============================================================
   3. 大阵根节点
   ============================================================ */
const arrayGroup = new THREE.Group();
scene.add(arrayGroup);


export { arrayGroup };

